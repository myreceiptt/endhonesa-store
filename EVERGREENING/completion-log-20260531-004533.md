# EVERGREEN Completion Log — endhonesa-store

## Scope

- Repository: `endhonesa-store`
- Branch: `stacks-evergreen-store-20260531`
- Base branch: `main`
- Repo class: Class A — App Repo
- Runtime target: Node.js 24.x
- Package manager: npm 11.x

## Baseline

- Branch `main` was clean and up to date with `origin/main`.
- The repo used `package-lock.json`, so npm remains the package manager.
- Baseline install/build worked, but `npm audit --audit-level=moderate` reported 32 vulnerabilities including critical advisories in old Next.js and transitive Web3 dependencies.
- Baseline `npm run build` emitted a `pino-pretty` optional dependency warning from the WalletConnect/thirdweb stack.

## Monthly and Quarterly Maintenance

- Added `.nvmrc` with Node.js 24.
- Added `engines.node` and `engines.npm` metadata.
- Added `packageManager: npm@11.8.0`.
- Upgraded Next.js from 15.0.3 to 16.2.6.
- Upgraded React and React DOM from 18.x to 19.2.6.
- Upgraded thirdweb to 5.120.0.
- Upgraded TypeScript to 6.0.3.
- Upgraded Tailwind CSS from 3.x to 4.3.0.
- Replaced Tailwind v3 PostCSS wiring with `@tailwindcss/postcss`.
- Migrated global CSS to Tailwind CSS 4 import and theme token syntax.
- Migrated lint script from removed `next lint` flow to `eslint .`.
- Added flat ESLint config using `eslint-config-next` 16.2.6.
- Pinned `eslint` at 9.39.4 because ESLint 10 is not fully accepted by the current Next ESLint plugin ecosystem.
- Pinned `@types/node` at 24.12.4 because Node.js 24.x is the runtime target.
- Added `pino-pretty` to satisfy the optional dependency path used by the WalletConnect/thirdweb stack during build tracing.
- Removed obsolete `autoprefixer`.
- Removed obsolete Tailwind v3 config file.
- Added `.env.example` with `NEXT_PUBLIC_TW_CLIENT_ID=`.
- Added `vercel.json` with explicit npm install/build commands.
- Updated README with runtime, env, lint/build, and Evergreen notes.

## Code Adjustments

- Removed server-layout access to `window` from `src/app/layout.tsx`.
- Kept dark mode behavior client-side in `DarkModeProvider`.
- Adjusted `DarkModeProvider` for React 19 lint rules by avoiding function access before declaration and synchronous state updates in the initial effect.

## Validation

- `npm run lint`: passed.
- `npm run build`: passed.
- `npm outdated --long`: only reports intentional deferrals:
  - `@types/node` latest is 25.x, deferred because runtime target is Node.js 24.x.
  - `eslint` latest is 10.x, deferred because `eslint-config-next` 16.2.6 transitively uses plugins that still peer against ESLint 9.
- `npm audit --audit-level=moderate`: reduced from 32 vulnerabilities to 25 moderate vulnerabilities.

## Remaining Audit Notes

- Remaining audit findings are transitive to the Next.js and Web3/WalletConnect/thirdweb ecosystem.
- `npm audit fix --force` proposes breaking downgrades such as `next@9.3.3` or `thirdweb@0.13.56`, so those fixes were intentionally not applied.
- Direct dependencies are already upgraded to their latest stable versions available during this run.

## Result

- Evergreen mode completed for this branch.
- No feature behavior was intentionally changed.
- The branch is ready for manual review, deployment check, commit, and merge handling.
